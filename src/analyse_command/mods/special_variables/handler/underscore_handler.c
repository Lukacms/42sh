/*
** EPITECH PROJECT, 2022
** 42sh
** File description:
** underscore_handler
*/

#include <stdlib.h>
#include "mysh.h"

char *underscore_handler(shell_t *shell, char *line)
{
    char *new = NULL;

    if (!shell || !line)
        return NULL;
    return new;
}
